local Material = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kinlei/MaterialLua/master/Module.lua"))()

local UI = Material.Load({
     Title = "Dark Hub - Credits",
     Style = 3,
     SizeX = 400,
     SizeY = 100,
     Theme = "Mocha"
})

local Page = UI.New({
    Title = "Loader"
})

Page.Button({
    Text = "Made by Universe11",
    Callback = function()
       print("pack do pe")
    end
})

Page.Button({
    Text = "Close Credit",
    Callback = function()
       loadstring(game:HttpGet("https://raw.githubusercontent.com/Entoy77/Dark-Hub/main/UpdateScriptRoom.lua"))()
    end
})
